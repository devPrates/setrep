#
# Fernanda Personal - Variáveis de Ambiente (exemplo)
# NUNCA colocar valores reais neste arquivo. Copie para `.env` e preencha localmente.
#

# Banco de Dados (Prisma + PostgreSQL)
DATABASE_URL=

# NextAuth (Sessões persistentes)
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# Email (opcional para magic-link)
AUTH_EMAIL_SERVER=
AUTH_EMAIL_FROM=

# Ambiente de execução
NODE_ENV=

# Configurações públicas do App
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_APP_NAME=Fernanda Personal

# Observação:
# - Garanta que cookies de sessão sejam configurados como Secure/HttpOnly/SameSite apropriado.
# - Verifique variáveis antes de executar. Não commitar `.env` com valores reais.